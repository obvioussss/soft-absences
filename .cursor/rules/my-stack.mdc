md
Description: Project tech stack and infra
Auto Attach: *.py, *.md, static/**/*.js, static/**/*.html

# Technical stack

- Python (FastAPI + SQLAlchemy) backend
- Frontend static HTML/JS served via Vercel routing
- PostgreSQL/MySQL/SQLite supported via SQLAlchemy
- Separate DBs for dev, test, prod
- No JSON file storage for persistence
- Tests: pytest
description: Keep infra minimal, prefer managed services, no vendor lock-in specifics here
globs: "**/*"
alwaysApply: true
---
