# 🚀 Configuration de production - Soft Absences
# Copiez ce fichier vers .env.production et remplissez les valeurs

# =============================================================================
# CONFIGURATION OBLIGATOIRE
# =============================================================================

# Environnement
ENVIRONMENT=production

# Sécurité JWT
SECRET_KEY=votre-clé-secrète-très-sécurisée-ici
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# CONFIGURATION EMAIL (OPTIONNEL)
# =============================================================================

# Configuration SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=votre-email@gmail.com
SMTP_PASSWORD=votre-mot-de-passe-app

# Configuration Resend (alternative à SMTP)
RESEND_API_KEY=votre-clé-api-resend
RESEND_FROM_EMAIL=noreply@votre-domaine.com

# Email d'expédition par défaut
EMAIL_FROM=votre-email@gmail.com

# =============================================================================
# CONFIGURATION GOOGLE CALENDAR (OPTIONNEL)
# =============================================================================

# Credentials Google Calendar (JSON encodé en base64)
# GOOGLE_CALENDAR_CREDENTIALS supprimé

# =============================================================================
# CONFIGURATION CORS (OPTIONNEL)
# =============================================================================

# Origines CORS autorisées (séparées par des virgules)
CORS_ORIGINS=https://votre-domaine.com,https://www.votre-domaine.com

# =============================================================================
# CONFIGURATION BASE DE DONNÉES (OPTIONNEL)
# =============================================================================

# URL de la base de données (par défaut SQLite en mémoire pour Vercel)
DATABASE_URL=sqlite:///:memory:

# =============================================================================
# NOTES IMPORTANTES
# =============================================================================

# 1. SECRET_KEY : Générer une clé sécurisée avec :
#    python -c "import secrets; print(secrets.token_urlsafe(32))"

# 2. Pour Gmail, utiliser un mot de passe d'application :
#    - Aller dans les paramètres Google
#    - Sécurité > Connexion à Google > Mots de passe d'application

# 3. Pour Google Calendar :
#    - Créer un projet dans Google Cloud Console
#    - Activer l'API Google Calendar
#    - Créer des credentials de service
#    - Encoder le JSON en base64

# 4. Variables d'environnement Vercel :
#    - Aller dans le dashboard Vercel
#    - Settings > Environment Variables
#    - Ajouter chaque variable 